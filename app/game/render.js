function render(){
  // layer.debug = true;
  // if(snowBossLayer != undefined){
  //   snowBossLayer.debug = true;
  // }

  // if(fireball != undefined){
  //   game.debug.body(fireball);
  // }

  // layer.debug = true;
}
